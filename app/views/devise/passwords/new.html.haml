.text-center.border.py-5.w-75.mx-auto
  %h1.mb-4 Forgot your password?
  = form_for(resource, as: resource_name, url: password_path(resource_name), data: { turbo: false }, method: :post) do |f|
    = render 'devise/shared/error_messages', resource: resource
    = f.label :email
    = f.email_field :email, autofocus: true, class: 'form-control w-75 mx-auto'
    = f.submit 'Send me instructions', class: 'btn btn-outline-success mt-4'
  .d-flex.justify-content-center.mt-4
    = link_to 'Log in', new_session_path(resource_name), class: 'btn btn-outline-success mx-2'
    = link_to 'Sign up', new_registration_path(resource_name), class: 'btn btn-outline-success mx-2'
